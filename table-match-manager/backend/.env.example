# Database configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=table_user
DB_PASSWORD=table_password
DB_NAME=table_match_db

# Server configuration
PORT=3000

# For Docker development setup:
# 1. Start MySQL: docker-compose up -d mysql
# 2. Use the values above
# 3. Database will be auto-created with sample data

# For production setup:
# Update these values with your production database credentials